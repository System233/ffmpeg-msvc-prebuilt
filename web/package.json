{
  "name": "ffmpeg-msvc-prebuilt-site",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "tsx scripts/generate-content.ts",
    "render": "tsx scripts/render-locales.ts",
    "build": "npm run generate && npm run render && vitepress build",
    "dev": "npm run generate && npm run render && vitepress dev",
    "preview": "vitepress preview"
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@types/node": "^22.0.0",
    "js-yaml": "^4.2.0",
    "mustache": "^4.2.0",
    "tailwindcss": "^4.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0",
    "vitepress": "^1.6.0",
    "vue-i18n": "^10.0.0",
    "@iconify/vue": "^4.2.0",
    "@iconify-json/fa6-solid": "^1.2.0"
  }
}
